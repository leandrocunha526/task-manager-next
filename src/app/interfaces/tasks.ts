export default interface Tasks {
    id: number;
    title: string;
    description: string;
    priority: boolean;
    state: string;
    createdAt: string;
    updatedAt: string;
    listId: number;
}
